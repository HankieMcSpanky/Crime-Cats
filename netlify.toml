[build]
  publish = "build/web"
  # Build is handled by GitHub Actions, skip Netlify's build
  ignore = "exit 0"

[[headers]]
  for = "/*"
  [headers.values]
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Embedder-Policy = "require-corp"
